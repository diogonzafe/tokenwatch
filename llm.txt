# @diogonzafe/tokenwatch

## What this library does
Transparent TypeScript wrapper that intercepts LLM API calls (OpenAI, Anthropic, Google Gemini, DeepSeek) and tracks token usage and cost in real-time by session, user and model. Non-blocking — track() is synchronous; report/reset/export methods are async. No changes required to existing code.

## Package
- npm: @diogonzafe/tokenwatch
- Node 20+, TypeScript 5.x, ESM + CJS dual build
- One runtime dependency: zod (config validation)
- Peer deps (all optional): openai, @anthropic-ai/sdk, @google/generative-ai, better-sqlite3, pg, mysql2, mongodb

## Public API

### createTracker(config?) → Tracker
Config fields (all optional):
- storage: 'memory' | 'sqlite' | IStorage  — default 'memory'; pass an IStorage instance for Postgres/MySQL/MongoDB
- alertThreshold: number                    — USD, fires webhook once when exceeded
- webhookUrl: string                        — Discord/Slack webhook URL
- syncPrices: boolean                       — fetch remote prices on startup (default true)
- customPrices: Record<string, { input: number, output: number, maxInputTokens?: number }>

### Tracker methods
- track(entry)                              — called by providers, not by user (synchronous)
- getModelInfo(model: string) → ModelPrice | null  — prices + maxInputTokens, null if unknown (synchronous)
- getReport() → Promise<Report>
- reset() → Promise<void>
- resetSession(sessionId: string) → Promise<void>
- exportJSON() → Promise<string>                      — full report as JSON string
- exportCSV() → Promise<string>                       — all raw UsageEntry rows as RFC 4180 CSV; columns: timestamp,model,inputTokens,outputTokens,reasoningTokens,costUSD,sessionId,userId,feature

### Report shape
{
  totalCostUSD: number,
  totalTokens: { input: number, output: number },
  byModel: Record<string, { costUSD, calls, tokens: { input, output, reasoning } }>,
  bySession: Record<string, { costUSD, calls }>,
  byUser: Record<string, { costUSD, calls }>,
  byFeature: Record<string, { costUSD, calls }>,
  period: { from: string, to: string }
}

### Provider wrappers
- wrapOpenAI<T>(client: T, tracker): T
- wrapAnthropic<T>(client: T, tracker): T
- wrapGemini<T>(client: T, tracker): T
- wrapDeepSeek<T>(client: T, tracker): T  — alias for wrapOpenAI

All wrappers:
- Accept __sessionId, __userId, and __feature as extra fields in the request params
- Strip those fields before forwarding to the real API
- wrapOpenAI covers: chat.completions.create (streaming + non-streaming) and embeddings.create
- wrapOpenAI reasoning tokens: reads completion_tokens_details.reasoning_tokens; priced as output tokens (separate from completion_tokens in o1/o3/o4)
- wrapAnthropic reasoning tokens: approximate (thinking block chars ÷ 4); informational only — thinking is already included in outputTokens, not added to cost
- wrapGemini: __feature is passed on getGenerativeModel() params, not per-call
- Return the exact same type as the input client (generics preserve types)
- Track cost after a successful call; do nothing if the API throws
- Support streaming
- record() is fire-and-forget — a storage failure never interrupts the API call

## UsageEntry fields
model, inputTokens, outputTokens, reasoningTokens? (OpenAI: separate from outputTokens, priced as output; Anthropic: approximate, informational only), costUSD, sessionId?, userId?, feature?, timestamp

## IStorage interface
All methods are sync-or-async (return T or Promise<T>):
- record(entry: UsageEntry): void | Promise<void>
- getAll(): UsageEntry[] | Promise<UsageEntry[]>
- clearAll(): void | Promise<void>
- clearSession(sessionId: string): void | Promise<void>

Built-in implementations: MemoryStorage (default), SqliteStorage (requires better-sqlite3)

## Database adapters (import from @diogonzafe/tokenwatch/adapters)

### PostgresStorage
- Peer dep: pg >= 8
- Constructor: new PostgresStorage(pool: pg.Pool | pg.Client)
- Extra method: migrate() → Promise<void>  — creates tokenwatch_usage table
- Schema: id BIGSERIAL, model TEXT, input_tokens INT, output_tokens INT, cost_usd NUMERIC, session_id TEXT, user_id TEXT, timestamp TIMESTAMPTZ

### MySQLStorage
- Peer dep: mysql2 >= 3
- Constructor: new MySQLStorage(pool: mysql2.Pool | mysql2.Connection)
- Extra method: migrate() → Promise<void>  — creates tokenwatch_usage table
- Schema: id BIGINT UNSIGNED AUTO_INCREMENT, model VARCHAR(255), input_tokens INT, output_tokens INT, cost_usd DECIMAL(18,8), session_id VARCHAR(255), user_id VARCHAR(255), timestamp DATETIME(3)

### MongoStorage
- Peer dep: mongodb >= 6
- Constructor: new MongoStorage(db: Db)
- Extra method: createIndexes() → Promise<void>  — creates indexes on timestamp, sessionId, userId, model
- Collection name: tokenwatch_usage

Custom adapter: any object implementing IStorage works as storage value.

## Price resolution order
1. customPrices (user override)
2. Remote prices.json from GitHub (cached 24h at ~/.tokenwatch/prices.json)
3. Bundled prices.json (always present as fallback)
Unknown model → cost = $0 + console.warn

Prices are in USD per 1 million tokens.
Cost formula: (inputTokens / 1_000_000) * price.input + (outputTokens / 1_000_000) * price.output

Model alias matching: exact match first, then prefix match (e.g. "gpt-4o-2024-11-20" matches key "gpt-4o").

## Bundled models (prices.json)
200+ models across OpenAI, Anthropic, Gemini, DeepSeek — auto-updated daily.
Each model entry: { input: number, output: number, maxInputTokens?: number }
Source: https://github.com/BerriAI/litellm (model_prices_and_context_window.json)

Example entries:
gpt-4o:            { input: 2.50,  output: 10.00, maxInputTokens: 128000 }
gpt-4o-mini:       { input: 0.15,  output: 0.60,  maxInputTokens: 128000 }
claude-opus-4-6:   { input: 5.00,  output: 25.00, maxInputTokens: 1000000 }
claude-sonnet-4-6: { input: 3.00,  output: 15.00, maxInputTokens: 1000000 }
claude-haiku-4-5:  { input: 1.00,  output: 5.00,  maxInputTokens: 1000000 }
gemini-2.5-pro:    { input: 1.25,  output: 10.00, maxInputTokens: 1048576 }
gemini-2.5-flash:  { input: 0.30,  output: 2.50,  maxInputTokens: 1048576 }
deepseek-chat:     { input: 0.28,  output: 0.42,  maxInputTokens: 131072 }
deepseek-reasoner: { input: 0.28,  output: 0.42,  maxInputTokens: 131072 }

## File structure
src/
  types/index.ts          — all exported types (IStorage, Tracker, TrackerConfig, UsageEntry, Report, etc.)
  core/
    pricing.ts            — resolvePrice(model, layers), calculateCost(inputTokens, outputTokens, price)
    sync.ts               — getRemotePrices(), fetchRemotePrices(url?), loadCachedPrices()
    storage.ts            — MemoryStorage, SqliteStorage, createStorage(type)
    tracker.ts            — createTracker(config) — main entrypoint
  providers/
    openai.ts             — wrapOpenAI
    anthropic.ts          — wrapAnthropic
    gemini.ts             — wrapGemini
    deepseek.ts           — re-exports wrapOpenAI as wrapDeepSeek
  adapters/
    postgres.ts           — PostgresStorage (pg)
    mysql.ts              — MySQLStorage (mysql2)
    mongodb.ts            — MongoStorage (mongodb)
    index.ts              — barrel export
  index.ts                — public exports
bin/cli.ts                — CLI: sync | prices | report (reads ~/.tokenwatch/usage.db) | help
prices.json               — bundled pricing data (200+ models, updated daily)
scripts/scrape-prices.mjs — fetches LiteLLM JSON, run by GitHub Action
.github/workflows/sync-prices.yml — daily price sync (commits prices.json; npm publish is manual, code changes only)

## CLI
tokenwatch sync     — fetch and cache remote prices
tokenwatch prices   — list bundled models and prices
tokenwatch report   — read ~/.tokenwatch/usage.db and print cost breakdown (total, by model, by user, by session)
                      shows "No SQLite database found" if db doesn't exist, "No usage recorded yet" if empty
tokenwatch help     — help

## Config validation
zod schema validates TrackerConfig at createTracker() call time.
Throws "[tokenwatch] Invalid config:\n  <field>: <message>" on invalid input.
Invalid storage string (e.g. 'redis') throws; valid IStorage instances pass validation via structural check.

## SQLite
Optional. Requires better-sqlite3.
DB path: ~/.tokenwatch/usage.db
Falls back gracefully with clear error if better-sqlite3 not installed.

## Streaming
OpenAI: wraps AsyncIterable, captures usage from the last chunk (requires stream_options: { include_usage: true })
  If stream has no usage data: records $0 + console.warn mentioning include_usage
Anthropic: captures input_tokens from message_start event, output_tokens from message_delta event
Gemini generateContentStream: usage read from the response Promise after stream completes

## Privacy & Security
- Prompt and response content is never read or stored — only token counts and model names
- API keys are never accessed by tokenwatch
- All storage (SQLite, Postgres, MySQL, MongoDB) stays entirely in user's own infrastructure
- The Proxy wrapper makes no outbound network calls (only the price sync script fetches GitHub)
